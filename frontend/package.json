{
  "name": "closet-org-web",
  "private": true,
  "version": "1.0.0",
  "description": "Vitest unit tests for Closet-Org web frontend utilities",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
