# Copy to .env (gitignored) for local Expo / Expo Go.
# EAS production builds also set this in eas.json — .env is optional for store builds.

EXPO_PUBLIC_API_URL=http://193.122.156.36

# Local dev against your PC (uncomment instead):
# EXPO_PUBLIC_API_URL=http://192.168.1.50:8000
